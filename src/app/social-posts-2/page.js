import SocialPosts2Page from "@/sections/pages/SocialPosts2Page";

export const metadata = {
  title: "Social Posts 2",
  description:
    "The extended library of Randall Fryer campaign creatives — additional Instagram feed posts, story artboards, and multi-slide carousels, each rendered from its source HTML.",
};

export default function Page() {
  return <SocialPosts2Page />;
}
