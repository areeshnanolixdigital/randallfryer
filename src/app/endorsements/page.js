import EndorsementsPage from "@/sections/pages/EndorsementsPage";

export const metadata = {
  title: "Endorsements",
  description:
    "Doctors, deputies, shop owners, teachers, parents — a coalition of District 14 neighbors backing Adrian Vale for State Senate.",
};

export default function Page() {
  return <EndorsementsPage />;
}
