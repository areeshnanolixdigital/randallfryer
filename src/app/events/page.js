import EventsPage from "@/sections/pages/EventsPage";

export const metadata = {
  title: "Events",
  description:
    "Town halls, canvasses, roundtables, and rallies — every CapitalWatch event is open to the public.",
};

export default function Page() {
  return <EventsPage />;
}
